[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "dotnet SignalingServer.dll"
# Путь для проверки жив ли сервис. Railway будет стучаться сюда.
healthcheckPath = "/ping"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10